from setuptools import setup, find_packages

setup(
    name="vaultdb-client",
    version="1.1.1",
    description="Python TCP client for VaultDB protocol v2",
    packages=find_packages(),
    python_requires=">=3.8",
)
