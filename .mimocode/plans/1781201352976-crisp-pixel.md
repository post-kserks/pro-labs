# VaultDB — План дальнейших действий

## Текущий статус

### Выполнено:
- ✅ Group A: Trivial fixes (токен, Dockerfile, CountRows, RLock, WAL truncate)
- ✅ Group B: Medium tasks (YAML, HMAC, Broadcaster, Vacuum WAL, retry)
- ✅ Group C: WAL + атомарность (recovery, redo/undo, checkpoint)
- ✅ Phase 1: Data Integrity (WAL Recovery, Connection Pooling, Graceful Shutdown)
- ✅ Phase 2: Performance (Query Optimizer, B-tree Index, Buffer Pool, Concurrent Writes)
- ✅ Phase 3: SQL Completeness P0+P1+P2 (DISTINCT, JOINs, functions, CTE, MERGE, TRUNCATE)
- ✅ Phase 4: Security (TLS, Audit Logging)
- ✅ Phase 5: Operations (Dashboard, Log Rotation, Config Reload)
- ✅ Интеграционные тесты (30+ тестов)

### Осталось:
- 🔧 Парсер: DISTINCT, BETWEEN, LEFT/RIGHT/REVERSE functions
- 🔧 Парсер: CURRENT_DATE/TIME/TIMESTAMP, STRING_AGG
- 🔧 Парсер: CTE (WITH clause) парсинг
- 🔧 Парсер: INSERT ON CONFLICT парсинг
- 🔧 Парсер: RETURNING clause парсинг
- 🔧 Парсер: UPDATE FROM парсинг
- 🔧 Парсер: MERGE statement парсинг
- 🔧 Парсер: TRUNCATE TABLE парсинг
- 🔧 Парсер: SAVEPOINT парсинг
- 🔧 Executor: CTE execution (WITH clause)
- 🔧 Executor: MERGE execution
- 🔧 Executor: TRUNCATE execution
- 🔧 Executor: UPDATE FROM execution
- 🔧 Executor: Column constraints (NOT NULL, DEFAULT, UNIQUE, PRIMARY KEY)
- 🔧 Executor: SAVEPOINT execution
- 🔧 Storage: Persist B-tree index to disk
- 🔧 Storage: Persist Buffer Pool dirty pages
- 🔧 Tests: Доработать пропущенные тесты
- 🔧 Tests: Написать crash recovery тесты
- 🔧 Tests: Написать concurrent access тесты

---

## Приоритетные задачи

### P0: Критичные (нужны для корректной работы)

#### 1. Парсер: DISTINCT (2-3 часа)
- **Файл:** `parser/parser.go`
- **Задача:** Добавить поддержку `SELECT DISTINCT col FROM t`
- **Изменения:**
  - Добавить токен `DISTINCT` в лексер (если нет)
  - В `parseSelect` проверять `DISTINCT` после `SELECT`
  - Устанавливать `stmt.Distinct = true`

#### 2. Парсер: BETWEEN ... AND ... (2-3 часа)
- **Файл:** `parser/parser.go`
- **Задача:** Добавить поддержку `WHERE col BETWEEN 10 AND 20`
- **Изменения:**
  - Добавить токен `BETWEEN` в лексер (если нет)
  - В `parseExpression` обрабатывать `BETWEEN ... AND ...`
  - Создать `BetweenExpr` (уже есть в AST)

#### 3. Парсер: LEFT/RIGHT/REVERSE functions (2-3 часа)
- **Файл:** `parser/parser.go`
- **Задача:** Добавить поддержку `LEFT(str, n)`, `RIGHT(str, n)`, `REVERSE(str)`
- **Изменения:**
  - Добавить токены `LEFT`, `RIGHT`, `REVERSE` в лексер
  - В `parseFunctionCall` обрабатывать эти функции

#### 4. Парсер: CURRENT_DATE/TIME/TIMESTAMP (1-2 часа)
- **Файл:** `parser/parser.go`
- **Задача:** Добавить поддержку `SELECT CURRENT_DATE FROM t`
- **Изменения:**
  - Добавить токены `CURRENT_DATE`, `CURRENT_TIME`, `CURRENT_TIMESTAMP`
  - В `parseExpression` возвращать специальные AST узлы

#### 5. Парсер: STRING_AGG (2-3 часа)
- **Файл:** `parser/parser.go`
- **Задача:** Добавить поддержку `SELECT STRING_AGG(name, ',') FROM t`
- **Изменения:**
  - Добавить токен `STRING_AGG`
  - В `parseFunctionCall` обрабатывать с 2 аргументами

#### 6. Парсер: CTE (WITH clause) (4-6 часов)
- **Файлы:** `parser/parser.go`, `parser/ast.go`
- **Задача:** Добавить поддержку `WITH cte AS (SELECT ...) SELECT * FROM cte`
- **Изменения:**
  - Добавить токен `WITH` в лексер
  - В `parseSelect` обрабатывать `WITH` clause
  - Создать `CTEDefinition` и `CTEStatement` (уже есть в AST)
  - Интегрировать с executor

#### 7. Парсер: INSERT ON CONFLICT (3-4 часа)
- **Файлы:** `parser/parser.go`, `parser/ast.go`
- **Задача:** Добавить поддержку `INSERT ... ON CONFLICT DO NOTHING/UPDATE`
- **Изменения:**
  - Добавить токены `ON`, `CONFLICT`, `DO`, `NOTHING`
  - В `parseInsert` обрабатывать `ON CONFLICT` clause
  - Заполнить `OnConflictClause` (уже есть в AST)

#### 8. Парсер: RETURNING clause (2-3 часа)
- **Файлы:** `parser/parser.go`, `parser/ast.go`
- **Задача:** Добавить поддержку `INSERT ... RETURNING *`
- **Изменения:**
  - Добавить токен `RETURNING`
  - В `parseInsert`, `parseUpdate`, `parseDelete` обрабатывать `RETURNING`
  - Заполнить `Returning` поле (уже есть в AST)

#### 9. Парсер: UPDATE FROM (2-3 часа)
- **Файлы:** `parser/parser.go`, `parser/ast.go`
- **Задача:** Добавить поддержку `UPDATE t1 SET col = val FROM t2 WHERE ...`
- **Изменения:**
  - В `parseUpdate` обрабатывать `FROM` clause
  - Заполнить `FromTable` поле (уже есть в AST)

#### 10. Парсер: MERGE statement (4-6 часов)
- **Файлы:** `parser/parser.go`, `parser/ast.go`
- **Задача:** Добавить поддержку `MERGE INTO ... USING ... ON ... WHEN MATCHED THEN ...`
- **Изменения:**
  - Добавить токены `MERGE`, `INTO`, `USING`, `WHEN`, `MATCHED`, `NOT`
  - Создать `parseMerge` функцию
  - Заполнить `MergeStatement` (уже есть в AST)

#### 11. Парсер: TRUNCATE TABLE (1-2 часа)
- **Файлы:** `parser/parser.go`, `parser/ast.go`
- **Задача:** Добавить поддержку `TRUNCATE TABLE t`
- **Изменения:**
  - Добавить токен `TRUNCATE`
  - Создать `parseTruncate` функцию

#### 12. Парсер: SAVEPOINT (2-3 часа)
- **Файлы:** `parser/parser.go`, `parser/ast.go`
- **Задача:** Добавить поддержку `SAVEPOINT sp`, `ROLLBACK TO SAVEPOINT sp`, `RELEASE SAVEPOINT sp`
- **Изменения:**
  - Добавить токены `SAVEPOINT`, `RELEASE`
  - Создать функции парсинга

---

### P1: Средние задачи

#### 13. Executor: CTE execution (4-6 часов)
- **Файл:** `executor/cte.go`
- **Задача:** Реализовать выполнение CTE
- **Изменения:**
  - Интегрировать `CTEScope` с `ExecuteCTEStatement`
  - Поддержка materialized CTE
  - Кэширование результатов

#### 14. Executor: MERGE execution (4-6 часов)
- **Файл:** Новый `executor/commands_merge.go`
- **Задача:** Реализовать выполнение MERGE
- **Изменения:**
  - Создать `MergeCommand`
  - Реализовать WHEN MATCHED THEN UPDATE
  - Реализовать WHEN NOT MATCHED THEN INSERT

#### 15. Executor: TRUNCATE execution (2-3 часа)
- **Файл:** `executor/commands_ddl.go`
- **Задача:** Реализовать TRUNCATE TABLE
- **Изменения:**
  - Создать `TruncateCommand`
  - Удалить все строки из таблицы
  - Обновить каталог

#### 16. Executor: UPDATE FROM execution (3-4 часа)
- **Файл:** `executor/commands_dml.go`
- **Задача:** Реализовать UPDATE ... FROM
- **Изменения:**
  - Модифицировать `UpdateCommand`
  - Прочитать FROM таблицу
  - Объединить данные для обновления

#### 17. Executor: Column constraints (4-6 часов)
- **Файлы:** `executor/commands_ddl.go`, `storage/storage.go`
- **Задача:** Реализовать проверку constraints
- **Изменения:**
  - Проверка NOT NULL при INSERT/UPDATE
  - Проверка UNIQUE при INSERT/UPDATE
  - Проверка PRIMARY KEY при INSERT
  - DEFAULT значения при INSERT

#### 18. Executor: SAVEPOINT execution (3-4 часа)
- **Файлы:** `executor/commands_tx.go`, `executor/session.go`
- **Задача:** Реализовать SAVEPOINT
- **Изменения:**
  - Добавить `savepoints` map в Session
  - Реализовать `SAVEPOINT sp` — сохранить состояние
  - Реализовать `ROLLBACK TO SAVEPOINT sp` — восстановить состояние
  - Реализовать `RELEASE SAVEPOINT sp` — удалить savepoint

#### 19. Storage: Persist B-tree index (4-6 часов)
- **Файл:** `index/btree.go`
- **Задача:** Сохранять B-tree на диск
- **Изменения:**
  - Сериализация узлов в JSON
  - Десериализация при загрузке
  - fsync после изменений

#### 20. Storage: Persist Buffer Pool (3-4 часа)
- **Файл:** `storage/buffer_pool.go`
- **Задача:** Сбрасывать dirty pages при checkpoint
- **Изменения:**
  - Интеграция с checkpoint loop
  - Флаг dirty для каждой страницы
  - Periodic flush

---

### P2: Низкий приоритет

#### 21. Tests: Доработать пропущенные тесты (4-6 часов)
- Реализовать парсинг для пропущенных тестов
- Убрать `t.Skip()` из integration_test.go

#### 22. Tests: Crash recovery тесты (4-6 часов)
- Тесты на kill -9 во время операций
- Тесты на recovery после краша

#### 23. Tests: Concurrent access тесты (4-6 часов)
- Параллельные INSERT/SELECT
- Параллельные транзакции
- Deadlock detection тесты

#### 24. Security Audit (2-3 дня)
- Проверка SQL injection
- Проверка auth bypass
- Проверка TLS
- Проверка CORS

#### 25. Performance Benchmarks (2-3 дня)
- Benchmark для критических путей
- Load testing (1000+ connections)
- Memory profiling

---

## Порядок реализации

### Неделя 1: Парсер (P0)
1. DISTINCT
2. BETWEEN
3. LEFT/RIGHT/REVERSE
4. CURRENT_DATE/TIME/TIMESTAMP
5. STRING_AGG

### Неделя 2: Парсер (P0) + Executor (P1)
6. CTE (WITH clause)
7. INSERT ON CONFLICT
8. RETURNING
9. UPDATE FROM
10. CTE execution

### Неделя 3: Executor (P1)
11. MERGE execution
12. TRUNCATE execution
13. Column constraints
14. SAVEPOINT execution

### Неделя 4: Storage (P1)
15. Persist B-tree index
16. Persist Buffer Pool

### Неделя 5: Tests (P2)
17. Доработать пропущенные тесты
18. Crash recovery тесты
19. Concurrent access тесты

### Неделя 6: Audit & Benchmarks (P2)
20. Security Audit
21. Performance Benchmarks

---

## Критические файлы для изменений

### Парсер:
- `server/internal/parser/parser.go` — основной парсер
- `server/internal/parser/lexer.go` — лексер (токены)
- `server/internal/parser/ast.go` — AST узлы

### Executor:
- `server/internal/executor/commands_select.go` — SELECT
- `server/internal/executor/commands_dml.go` — INSERT/UPDATE/DELETE
- `server/internal/executor/commands_ddl.go` — DDL
- `server/internal/executor/commands_tx.go` — транзакции
- `server/internal/executor/cte.go` — CTE
- `server/internal/executor/eval.go` — функции

### Storage:
- `server/internal/storage/page_engine.go` — page engine
- `server/internal/storage/buffer_pool.go` — buffer pool
- `server/internal/index/btree.go` — B-tree index

### Тесты:
- `server/internal/executor/integration_test.go` — интеграционные тесты

---

## Верификация

### Для каждой задачи:
1. `go build ./...` — компиляция
2. `go test ./... -count=1` — все тесты проходят
3. `go test ./... -race` — нет data races
4. `go vet ./...` — нет ошибок

### После всех изменений:
1. `go test ./... -v` — все тесты с verbose
2. `go test ./... -bench .` — benchmarks
3. Ручное тестирование: `go run ./cmd/vaultdb-server`
